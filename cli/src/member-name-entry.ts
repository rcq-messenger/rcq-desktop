// Bundle entry: the group member name order alone (#982), for the offline test
// (cli/test/member-name.mjs). Same purpose as routes-entry.ts: proven against
// the BUILT bundle, without a browser.

export { isNumberOnly, memberName, quotedAuthorNames } from '../../src/lib/member-name'
