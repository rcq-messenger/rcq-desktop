// Bundle entry: the thread's ship gate and carbon set alone (#1038, #1039), for
// the offline test (cli/test/ship-kinds.mjs). Same purpose as
// member-name-entry.ts: proven against the BUILT bundle, without a browser.

export { CARBON_KINDS, COMPOSED_KINDS, SHIPPABLE_KINDS } from '../../src/lib/ship-kinds'
