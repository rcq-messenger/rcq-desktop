// The CLI's own version — the release tag on rcq-messenger/rcq-cli minus the
// 'v'. Bump this together with the GitHub release; the update check below
// compares the two and tells the person how to catch up (the CLI has no
// self-update — it is one file installed by hand, and it stays that way).
export const CLI_VERSION = '0.2.17'
